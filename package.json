{
  "name": "react",
  "version": "1.0.0",
  "description": "`1`- JSX used to write html elements in JS and place them in inside virtual dom without any createElement or appendchild Elements\r      this is internally handle like this \r      ```\r       <div>\r         <h1> heading</h1>\r         <P> paragraph <p>\r       </div>\r      ```\r `2`- it converts HTML tags into react elements i.e translated into regular JS at runtime\r `3`- in JSX allow only one element in root if u want more element then put them inside a container.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "create-react-app": "^5.0.1",
    "react-icons": "^5.0.1"
  }
}
